<TreeView x:Class="DesktopApp.View.ProjectTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <TreeViewItem Name="projetItem" Header="Projet" IsExpanded="True">
        <TreeViewItem Name="serversItem" Header="Serveurs" IsExpanded="True">
            <TreeViewItem.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Insérer..." Command="{Binding CreateArduinoServer}"></MenuItem>
                </ContextMenu>
            </TreeViewItem.ContextMenu>
        </TreeViewItem>
        <TreeViewItem Name="viewsItem" Header="Vues" IsExpanded="True">
            <TreeViewItem.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Insérer..." Command="{Binding CreateDisplayContent}"></MenuItem>
                </ContextMenu>
            </TreeViewItem.ContextMenu>
        </TreeViewItem>
    </TreeViewItem>
</TreeView>
